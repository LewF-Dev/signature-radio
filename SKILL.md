# Signature Radio Repo Notes

- This container may not include common inspection tools such as `rg` or `file`; use `find`, `grep`, `ls`, and browser checks as fallbacks.
- The homepage hero uses unusually large desktop sizing and negative margins around `.hero-logo`; check the 1024px+ CSS before changing hero alignment.
